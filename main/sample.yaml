- number: 1   # worker process
    funcs:
    - func: "httpServer"
      mode: "TCP"
      bind: "0.0.0.0"
      port: 2000
      entry: test  # entry path

user:
  - entry: uniServ
    mode: "TCP"
    path: "db.sock"